{{>header}}

<div class="container-fluid containerTop">
  <section id="cursosAbiertos" class="container-fluid text-center">
    <h1>Cursos disponibles</h1>
      <ul class="row">
      {{#each listadoCursos}}
        {{#cursoDisponible this.estado value="disponible"}}
      <li class="col-md-3">
        <div class="informacionCurso">
          <figcaption>
              <figure>
                  <img src="/img/programas/{{this.categoria}}.jpg">
              </figure>
            </figcaption>
            <div class="datosdelCurso">
              <h3>{{this.nombre}}</h3>
              <p>Costo: <span>${{this.costo}}</span></p>
              <p> Modalidad: <span>{{this.modalidad}}</span></p>
              <button type="button" class="btn btn-primary" data-toggle="modal" data-target='#{{this.id}}'>Mas información</button>
              <div class="modal fade" id='{{this._id}}' tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                      <div class="modal-dialog" role="document">
                        <div class="modal-content">
                          <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                              <span aria-hidden="true">&times;</span>
                            </button>
                          </div>
                          <div class="modal-body">
                            <h3>{{this.nombre}}</h3>
                            <p>Costo $: <span>{{this.costo}}</span></p>
                            <p>Modalidad: <span>{{this.modalidad}}</span></p>
                            <p>Duración : <span>{{this.duracion}}</span></p>
                            <div> 
                              <h4>Descripción:</h4>                            
                              <p class="descripcionCurso">{{this.descripcion}}</p>
                            </div>
                          </div>
                          <div class="modal-footer">
                            <form  name="{{this._id}}" action="/registrarseCurso" method="POST">
                            <input type="hidden" name="idCurso" value="{{this._id}}">
                            <input type="hidden" name="identidad" value="{{@root.datos.identidad}}">
                            <button class="btn-primary" type="submit">Inscribirme</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
            </div>
        </div>
      </li>  
      {{/cursoDisponible}}
    {{/each}}
    </ul>
  </section>
</div>

{{>footer}}